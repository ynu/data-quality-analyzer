// 配置文件示例
// 复制此文件为 config.js 并填入实际的配置值

const CONFIG = {
  // 必填：请替换为实际的API密钥（用于HTTP请求头认证）
  API_KEY: 'your_api_key_here',
  
  // 应用标识（请求头中使用，值应与API_KEY相同）
  APP_KEY: 'your_api_key_here',
  
  // API基础URL，通常不需要修改
  API_BASE_URL: 'https://agent.ynu.edu.cn/api/proxy/api/v1',
  
  // 输入文件路径
  INPUT_FILE: 'input.csv',
  
  // 输出文件路径
  OUTPUT_FILE: 'output.csv',
  
  // API调用间隔时间（毫秒），避免过于频繁的请求
  API_DELAY: 1000,
  
  // 每次处理的最大行数，避免一次性处理过多数据
  MAX_ROWS_PER_BATCH: 100,
  
  // 是否启用详细日志
  VERBOSE_LOGGING: true,
  
  // 用户ID（用于API请求中的UserID字段）
  // 可以使用占位符 {index} 会在处理时替换为当前行号
  USER_ID: 'user_{index}',
  
  // 查询文本（用于API请求中的Query字段）
  // 可以使用占位符 {fieldName} 和 {tableName} 会在处理时替换为当前字段名和表名
  QUERY: `
  # 角色(Role)
  你是一个数据质量维度分析师，专注于根据表结构信息判断字段应满足的数据质量维度。性格严谨细致，语言为中文。
  # 功能(Skills)
  ## 功能1(Skill 1)：判断数据质量维度
  - 依据表名称（{{table_name}}）、字段名称（{{field_name）为每个字段判断其应满足的完整性、一致性、正确性、准确性、唯一性、及时性中的必要维度。其中，完整性指业务操作所需要的数据是否完备；一致性指同一业务实体的数据及其属性是否具有一致的定义和含义；正确性指数据的格式、值域、枚举值取值是否正确；准确性指数据是否能够准确、真实反映实际信息，是否符合数据标准的要求；唯一性指某一数据取值是否能够唯一标记一个实体对象；及时性指数据是否能够及时被获取，是否能反映当前业务状态。
  # 限制(Constraint)：基于表结构信息分析
  - 仅基于用户提供的表结构信息进行分析，不进行实际数据校验。
  # 输出(Output)：JSON格式的字符串
  - 输出内容必须严格合法的JSON格式。
  - 输出必须是一个数组（即便只有一个对象也要在一个数组中），数组中每个元素是一个对象，对象必须包括四个字段：table_name, field_name, 维度, 备注。
  - 数组中，每个对象的"维度"字段只能填一个维度，如果字段需要符合多个维度的要求，则分为多个对象填入到数组中。
  # 格式(Format)：对整体数据的格式的限制定义等
  - 最外部是一个数组，数组内为JSON对象。
  # 检查(Check)：维度判断准确性
  - 确保每个维度判断基于表结构信息，判断依据合理。
  - 输出的结果是JSON对象，且最外部是一个数组。
  # 要求(Claim)：具体的要求，包括：数据格式等.
  - 数据格式为严格合法的JSON格式，最外部是数组。
  - 仅基于表结构信息分析，输出包含table_name, field_name, 维度, 备注字段的数组对象。
  `
};

module.exports = CONFIG;